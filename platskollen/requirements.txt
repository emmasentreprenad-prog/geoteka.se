# Platskollen — Trust Stage 1 dependencies.
# Pinned loosely (>=) rather than exactly; tighten once a real deployment
# target is chosen.

geopandas>=0.14
shapely>=2.0
pyogrio>=0.7
rasterio>=1.3
numpy>=1.26
pyproj>=3.6
whitebox>=2.3

# dev / test
pytest>=8.0
